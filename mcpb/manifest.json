{
  "manifest_version": "0.2",
  "name": "tvtropes-mcp",
  "version": "0.1.0",
  "description": "TVTropes local mirror + MCP server — background scraper + query interface",
  "author": { "name": "Sandra Schieder" },
  "server": {
    "type": "python",
    "entry_point": "src/tvtropes_mcp/__main__.py",
    "mcp_config": {
      "command": "uv",
      "args": ["run","--directory","${PWD}","python","-m","tvtropes_mcp"],
      "env": {
        "PYTHONPATH": "${PWD}/src",
        "PYTHONUNBUFFERED": "1"
      }
    }
  },
  "tools": [
    { "name": "tvtropes-mcp", "description": "TVTropes local mirror + MCP server — background scraper + query interface" }
  ]
}
